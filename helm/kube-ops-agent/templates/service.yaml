apiVersion: v1
kind: Service
metadata:
  name: {{ include "kube-ops-agent.fullname" . }}
  labels:
    {{- include "kube-ops-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kube-ops-agent.selectorLabels" . | nindent 4 }}
